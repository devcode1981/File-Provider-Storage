#!/usr/bin/env ruby

gitlab_repo_ruby_version = `cat .ruby-version`.strip

expected = Gem::Version.new(gitlab_repo_ruby_version)
actual = Gem::Version.new(RUBY_VERSION)

if actual < expected
  abort "Please install Ruby version #{gitlab_repo_ruby_version} or higher"
end

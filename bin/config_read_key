#!/usr/bin/env ruby

require_relative '../lib/gdk/config'

def read_key
  return ARGV[0] unless ARGV.empty?

  STDERR.print '> Please enter a key name: ' if $stdin.tty?
  STDIN.gets.chomp
end

config = GDK::Config.new
puts config.read_key(read_key).to_s
